void setup() {
  /* Task 2
  rectMode(CENTER);
  fill(255 / 2);
  rect(width / 2, height / 2, width * 0.75, height  * 0.75);
  */
}

void draw() {
  /* Task 3
  background(205);
  textAlign(CENTER);
  textSize(20);
  fill(0);
  text("mouse", mouseX, mouseY);
  */
}

/* Task 1 - random rectangles
void keyPressed() {
  float rx = random(5,40);
  float ry = random(5,40);
  float rwidth = random(5,40);
  float rheight = random(5,40);
        
  rect(rx, ry, rwidth, rheight);
}

*/

/* Task 2
void mousePressed() {
  background(205);
  float rr = random(0,255);
  float rg = random(0,255);
  float rb = random(0,255);
  
  rect(width / 2, height / 2, width * 0.75, height  * 0.75);
  fill(rr, rg, rb);
}
*/
